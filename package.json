{
  "name": "pipe-loss-calculator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "node build.mjs --serve"
  },
  "dependencies": {
    "@preact/signals": "^1.3.0",
    "@shoelace-style/shoelace": "^2.19.1",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
